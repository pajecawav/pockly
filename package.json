{
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"dev": "yarn workspace @pockly/web run dev",
		"prepare": "is-ci || husky install"
	},
	"lint-staged": {
		"*.{js,jsx,ts,tsx,json,md,yml,css}": [
			"prettier --write"
		]
	},
	"devDependencies": {
		"husky": "^8.0.1",
		"is-ci": "^3.0.1",
		"lint-staged": "^12.4.1",
		"prettier": "^2.5.0"
	}
}
